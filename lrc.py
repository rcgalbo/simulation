# left right center simulation

# simulate game play of lcr to estimate the game length
# setup spec - 6 players, 3 $ each
# -> generalize to x players with y dollars
from random import choice

def turn(i, player, roll):
    

def simulate_lcr(players = 6, dollars = 3):

    players = [dollars for _ in range(player)]
    winner = False
    roll_outcomes = [1,1,2,2,3,3]
    center = 0
    while winner == False:
        for i, player in enumerate(players):
            
